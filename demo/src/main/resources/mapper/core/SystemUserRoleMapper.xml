<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.fxly.demo.api.core.mapper.SystemUserRoleMapper">

    <select id="getRoleListByUserId" resultType="com.fxly.demo.api.core.entity.SystemRole">
        SELECT r.*
        FROM system_user_role ur
                 LEFT JOIN system_role r on r.id = ur.role_id
        where user_id = #{userId}
    </select>

    <insert id="grantRole">
        insert into system_user_role(user_id, role_id)
        values
        <foreach collection="roleIds" item="roleId" separator=",">
            (#{userId}, #{roleId})
        </foreach>
    </insert>

    <delete id="deleteBatch">
        delete from system_user_role
        where user_id = #{userId}
        and role_id in
        <foreach collection="roleIdList" item="roleId" open="(" close=")" separator=",">
            #{roleId}
        </foreach>
    </delete>

</mapper>